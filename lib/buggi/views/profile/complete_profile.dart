import 'package:app/common_libs.dart';

class CompleteProfileSect extends StatelessWidget {
  const CompleteProfileSect({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
